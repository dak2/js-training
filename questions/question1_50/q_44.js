const x = new Boolean(false)
// if文の式として渡すと実行されるか答えなさい
// https://developer.mozilla.org/ja/docs/Web/JavaScript/Reference/Global_Objects/Boolean

if (x) {
  console.log("true")
}

// 値が undefined や null でないオブジェクトは、値が false の Boolean オブジェクトも含めて、条件文に通されると全て true に評価